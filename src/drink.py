class Drink:
    def __init__(self, name, price): # alochol_level would also need added here
        self.name = name  
        self.price = price
        # self.alcohol_level = alcohol --- this would be different for each drink. each drink object set up in test file would need alcohol level added.